# ============================================================================
# ユーティリティ
# ============================================================================

import math

def is_equal(a, b):
  """aとbが等しいかどうかを確認する (a == b)

  Args:
    a(float): a
    b(float): b

  Returns:
    bool: aとbが等しいかどうかの値
  """
  return math.isclose(a, b)
